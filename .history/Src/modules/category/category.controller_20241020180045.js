export const getAll = (req,res)=>{

    return res.json({message:"success"});
}
export const create = (req,res)=>{


      const name = req.body.name.toLower
    return res.json({message:"success"});
}
